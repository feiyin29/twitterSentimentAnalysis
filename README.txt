------- Before run the program install this -------
pip install twitter 
pip install tweepy 
pip install pymongo 
pip install wordclound
pip install textblob
pip install clean-text
pip install pandas
pip install streamlit

------- run file with command -------
streamlit run twitterSentiment.py

